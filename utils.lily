import "utils/ensure_same_size"
import "utils/left_pad"
import "utils/to_list"

class Utils {
  public var @ensure_same_size_list = ensure_same_size.ess_list
  public var @ensure_same_size_str = ensure_same_size.ess_str
  public var @left_pad_list = left_pad.lp_list
  public var @left_pad_string = left_pad.lp_str
  public var @to_char_list = to_list.char_list
}